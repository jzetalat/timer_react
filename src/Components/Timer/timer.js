import React, { useState, useEffect } from "react";


const Timer = () => {

    useEffect(() => {        
    });

    return (
        <div className="timer-container">
            <h2> Timer Project init</h2>
        </div>
    );
};

export default Timer;
